framework:
    notifier:
        chatter_transports:
        texter_transports:
        channel_policy:
            # utilise chat/slack, chat/telegram, sms/twilio ou sms/nexmo
            urgent: ['email']
            high: ['email']
            medium: ['email']
            low: ['email']
        admin_recipients:
            - { email: admin@example.com }
